steal(
        
    'can', 
    function(can) {

        var task = {
            name:       'task',
            complete:   false,
            notes:      ''
        };

        return can.Map(task);

})

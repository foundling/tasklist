steal(

    'can',

    function(
        can, 
        dashboardView
    ) {

        can.Component.extend({
            tag: 'app-container',
            viewModel: {
            },
            events: {
            }
        });

});

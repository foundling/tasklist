<div class="task-wrapper">
    <!--
    <input 
        can-click="toggleComplete" 
        class="task-complete" 
        type="checkbox"/>
    -->

    <input 
        can-value="text"
        can-focus="toggleEditing" 
        can-blur="toggleEditing" 
        value="{{text}}"
        class="task-input {{#editing}}editing{{/editing}}"/>
</div>

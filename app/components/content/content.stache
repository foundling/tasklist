<div class="app-content">

    {{#if title}}

        <h1 class="app-content-title">{{title}}</h1>

    {{/else}}
        <div class="app-content-main">
            <content>
        </div>

    {{/if}}
</div>

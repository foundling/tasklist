<div class="app-content-window">
    {{#is view 'title'}}
        <h1 can-click="switchView 'multilist'" class="app-content-window-title">{{title}}</h1>
    {{/else}}
        <content/>
    {{/if}}
</div>
